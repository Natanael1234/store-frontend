import { ColSize } from '../../../../types/col-size.type';

export interface AbstractFormElementOptions {
    id?: string;
    colSize?: ColSize;

    // TODO:
    // class
    // style
    // hidden
}
