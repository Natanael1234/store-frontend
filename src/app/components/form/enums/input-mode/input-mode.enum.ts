/**  Type of browser's virtual keyboard. */
export enum InputMode {
    none = 'none',
    text = 'text',
    decimal = 'decimal',
    numeric = 'numeric',
    tel = 'tel',
    email = 'email',
    url = 'url',
    search = 'search',
}
