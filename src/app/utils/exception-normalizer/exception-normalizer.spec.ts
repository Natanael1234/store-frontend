import { normalizeException } from './exception-normalizer';

describe('normalizeException', () => {
    it('should be defined', () => {
        expect(normalizeException).toBeDefined();
    });
    // TODO: test
});
