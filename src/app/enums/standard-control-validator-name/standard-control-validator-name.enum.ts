export enum StandardControlValidatorError {
    min = 'min',
    max = 'max',
    required = 'required',
    requiredTrue = 'requiredTrue',
    email = 'email',
    minlength = 'minlength',
    maxlength = 'maxlength',
    nullValidator = 'nullValidator',
}
